Get files in your server on your local computer(without intermediary):
```bash
scp user@YOUR_IP:/file/path/ ./
```
Send your files in your server with local computer:
```bash
scp /file/path/ user@YOUR_IP:/file/path/
```
